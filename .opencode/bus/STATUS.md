# Bus STATUS — board

Fonte de verdade para **claims** e para o **estado atual**. Histórico vive no AMQ, não aqui.
Agentes: `coordinator`, `ballistics`, `player-rig`, `range`, `npc-body`, `meta`, `testkit`, `spotter`, `qa`.
Protocolo: AGENTS.md regra 7 (drenar → claim → **responder ao remetente** → relato em 5 partes).

## EM VOO (claimed)

> 🔒 **REGRA (coordinator 2026-09-21T18:02Z): 100% do Godot DIRETO via `tools/godot-lock.sh`.** O `.godot/` é
> compartilhado e imports concorrentes TRAVAM (reproduzido: `reimport | pistol_9mm_albedo.png` travado; gate não
> roda; `qa_audit` estourou 300s). O `tools/godot-lock.sh` (`424e369`, testkit) usa o **MESMO lockfile** do
> `verify-all` (`/tmp/shooter/verify-all.<cksum(ROOT)>.lock`) → import direto e gate NUNCA rodam juntos. O
> `verify-all` NÃO precisa do wrapper (pega o lock por dentro); o `gate_import` tem `timeout 300` + retry +
> limpeza de resíduo 0-byte (`8e8b67b`/`e6623d6`). Uso: `tools/godot-lock.sh [--clean-tmp] --headless --path .
> --import` · `AGENTS.md` regra `37092ea`. Se precisar matar um Godot travado, **só o seu pid** (nunca `pkill`
> amplo).

| Item | Dono | Estado |
|---|---|---|
| **Commit da lane range** (gunsmith UI + hook + `mod_weapon`) | range | **DONE 2026-09-21T16:20Z — HEAD `20f812b`** (pai `c52d1ad` do meta). **Follow-up `d47b77a`**: rename do `display_name` da extração paga `"V-Ex"` -> `"Express"` + 2 comentários (`arena_blockout.tscn`, `player_profile.gd`) — regra de identidade, `grep V-Ex` zerado em `scenes/ src/ resources/ tools/` (sobra só `README.md:25`, lane docs). 16 arquivos: `scenes/{gunsmith_ui,faction,faction_registry,validate_factions}.gd` (+`.uid`), `scenes/{arena_manager,player_profile,extraction_point}.gd`, `resources/meta/factions/*.tres` (3), `src/meta/meta_profile.gd` (2 linhas, avisado ao meta por AMQ) e `project.godot` **só** com o bloco `mod_weapon` (escrito cirurgicamente: HEAD + 1 bloco; as outras mudanças no working tree são de outras lanes e ficaram fora). `sounds/**`, `export_presets.cfg` e `.github/workflows/ci.yml` já estavam commitados/limpos — nada a fazer neles. Gates: `verify-all --quick` PASS (152 scripts/0 parse errors, uid_tracking, assets 123/123 — `/tmp/shooter/range_verify_quick.log`); arena_blockout + debug_range bootam headless 0 erros; `qa --json` BLOCKER=0 MAJOR=48 == baseline |
| **Facção como DADO** (supersede o rename PMC/SCAV: Resource + registry, `PlayerProfile.faction` = String id, `ExtractionPoint.allowed_factions`) | range | **DONE 2026-09-21T16:20Z** — `scenes/faction.gd` (Resource: id/display_name/color/playable) + `scenes/faction_registry.gd` (load_dir/get_faction/require/has/all/ids/count/playable/display_name/color + `default_registry()`), pack `resources/meta/factions/{contractor,drifter,raider}.tres` (raider `playable=false`), `PlayerProfile.faction` String com migração explícita de save numérico legado + `push_error` em id desconhecido, `ExtractionPoint.allowed_factions: Array[String]` (vazio = qualquer) montando "apenas <display_name>" do dado. **Prova: `scenes/validate_factions.gd` 63/63 PASS** (pack de 3, ids únicos, pack de 5 in-memory sem mudar código, gate vazio aceita todos / restrito recusa os outros, migração legacy, guard de proper noun). Desvio do desenho: o registry expõe `get_faction(id)` em vez de `get(id)` (GDScript recusa sombrear `Object.get`). **Gate LIGADO pelo `testkit` (`tools/verify-all.sh:356`)** e **hardening `fc8d5dd`**: o harness referenciava `ExtractionPoint` em tempo de compilação, o que puxava `controller.gd` (`Debug` autoload, não registrado em modo `--script`) e deixava 3 linhas de erro no log de um gate verde; agora carrega em runtime -> log limpo, 63/63. NIT enviado ao `player-rig` (raiz no addon). |
| Fila player-rig: stealth (ruído → audição da IA), sway por ergonomia | player-rig | fila |
| **Mercado alcançável** (ligação mínima na arena: ponto de compra/venda + `offer_line()` no HUD) | meta → range | **DONE (range) 2026-09-21** — re-verificado por meta **15/15**; **spotter VERIFICOU 2026-09-21T14:30Z** (claim D na arena real: interact→painel→`offer_line()`→buy/sell→reload, 23/23 + captura `spot_market_panel.png`); evidência `/tmp/shooter/spot_arena_verify.txt` |
| **Flea market** (listings + taxa + escrow + expiração por contador de raids) | meta | DONE self 35/35; **pré-condição do guardrail 1 satisfeita** — **spotter VERIFICOU 2026-09-21T14:30Z** (HEAD `7d8b7b0`): atomicidade+quarentena (28 SIGKILL no meio da escrita, save sempre íntegro), escrow por **massa real**, economia exata (fee/piso, fair-price, barter, stock reset, seguro); 50/50 lógica + 28 SIGKILL; evidência `/tmp/shooter/spot_meta_verify.txt`, `spot_kill_verify.txt` |
| **Commit da lane do meta** (destrava CI) | meta | **DONE 2026-09-21T14:04Z** — HEAD **`50ab5ce`**: `src/meta` (52, validators + `.uid` + MetaService), `resources/meta` (7), `resources/medical` (9, dependência dos gates market/flea) e **só** a linha `Meta` autoload em `project.godot` (`MCPBridge` NÃO entrou). `c091a74` foi resetada pela própria lane que a criou (`84db948`). Gates: persistence 38/38 · progression 34/34 · market 45/45 · flea 35/35. Achado ao testkit: `.godot/` compartilhado + `godot --import`/harness concorrente gera **falso-FAIL** (uid cache stale, 7/45); serializar o CI |
| `tools/verify-all` (ponto único: import + harnesses + gate de qualidade, com exit code) | testkit | **DONE — 13 gates, PASS exit 0 no working tree**; `uid_tracking` VERDE (0 missing). Gates novos: `factions` (`scenes/validate_factions.gd`, 63, `16859b7`), `meta_flea` (35, `1b91e89` — 2º órfão da varredura do `meta`). `gate_harness` agora **re-importa + retenta 1x** (`.godot/global_script_class_cache.cfg` corrompido por `godot` CONCORRENTE de outras lanes → falso vermelho; meu `flock` só cobre `verify-all`). **BLOQUEIO ESTRUTURAL ainda ABERTO** (`meta`, confirmado por mim): HEAD do jogo com **0 gitlink** → clone limpo não tem `addons/` e o `verify-all` para no preflight (`f7a8730`); o PASS é **working-tree**, o CI ainda não roda. Commits meus (jogo): `36d0fe7`+`45e276b`+`5961118`+`f5b54f4`+`7d8b7b0`+`55aa423`+`f7a8730`+`70e455a`+`9c39055`+`7e44cec`+`16859b7`+`1b91e89` |
| Re-validar QA-001/QA-002 + auto-limpeza dos achados manuais no gate | qa | DONE 2026-09-21T11:17Z — QA-001/QA-002 RESOLVED (probes próprios em `tools/qa/probes/`); `--verify-manual` + `ignore.json`; matcher corrigido (3 classes morto-real/consumidor-pendente/falso-positivo); baseline v2.1.0, `--check` exit 0 (0 BLOCKER / 99 MAJOR) |
| **Audit pass pós-fixes: tool gating + baseline** | qa | **DONE 2026-09-21T13:59Z** — `debug-print` agora ignora prints sob `if OS.is_debug_build():` (falso-positivo que mantinha `arena_manager`/`debug_range` como MINOR); confirmados 6 itens do range (4 LIGAR com consumidor real, 2 campos mortos removidos) e os 2 BLOCKER do ballistics; baseline v2.1.0 re-snapshot deliberado **0/57/44/16**; `--check` exit 0 e import gate exit 0 (2 runs). Re-verificados 9 manuais do player-rig (QA-005 e **falso-positivo** confirmado; QA-008/009/014/015/016/017/018/024 resolved) -> atual **0/55/38/15**; QA-003 reatribuido player-rig -> ballistics e RESOLVED (addon `ea41124`: early-return `if freeze` antes das forcas -> regra 5); QA-010 RESOLVED (tree), QA-020/021 RESOLVED (addon `cf68d56`) -> atual **0/54/38/15**; spin-out cosmético QA-025 (ballistics, 6 headers) RESOLVED addon `ef4ebda` + QA-026 (player-rig, 3) RESOLVED (tree) -> ledger manual **27/27 resolved, 0 abertos**; baseline **0/51/37/14** mantido (nao aceitei o +1). range dedup: `_tick_footsteps` e o bloco de 20 linhas cairam, MAS o refactor de `_resolve_shot` expoz 2 duplicacoes reais (`arena_manager.gd:616===debug_range.gd:110` e `:629===:121`) -> range fechou a dedup arena<->debug_range: `ShotResolver.resolve` + `ShotResolver.apply_hit` (HitFlash.spawn/range_hit/audio/loot) compartilhados; `duplication` 2 -> 1 (so `cartridges_3d.gd:198===:219`) -> MAJOR 50, `--check` exit 0; baseline re-snapshotado **0/50/37/14**. `cartridges_3d.gd` (orfao, 1007 linhas) **DELETADO (range, autorizado)** -> `duplication` 0, god-object 3->2 -> MAJOR 48; baseline re-snapshotado **0/48/36/14**, `--check` exit 0. **Lane qa COMMITADA 94c42a5 + fbdcab1** (`tools/qa/*` + `docs/qa-baseline.json`) sob autorizacao do usuario -> gate 8 nao falha mais rc=3 em clone limpo; `fbdcab1` corrige o baseline p/ guardar o **vocabulario de 39 regras** (antes guardava so as que disparavam, o que perdoava regressao de MAJOR como 'taxonomy change'). `ip-name` estendido p/ **marcas reais de arma/acessorio** + scan de filenames (`2ac8f1f`) -> **14 BLOCKER** abertos (ballistics/range: resources + assets), gate vermelho DE PROPOSITO ate renomear; baseline nao elevado |
| Dívida de qualidade roteada pelo `qa` (por dono) | todos | em curso |
| **Dívida QA do player-rig** (QA-005/008/009/014/015/016/017/018/024/026 + ip-tarkov + icon default) | player-rig | **DONE — COMMITADO 2026-09-21T16:0xZ (addon `13b174c`)** — QA-008/014/015/016/017/018/024/026 + ip-tarkov; + default de `Item.icon` trocado do logo Godot p/ o placeholder gerado. QA-005/009 (container.gd) e o `get_mass()` de core/inventory/item.gd entraram no commit do `inventory-ux` `7d12d0f` (verificado em HEAD); os `.uid` já estavam em HEAD. Parse 149/0 PASS |
| ~~**BLOQUEIO cross-lane: gate `uid_tracking`**~~ | coordinator (dono do .uid) | **RESOLVIDO 2026-09-21T14:2xZ** — a lacuna do índice fechou: `git ls-files` do addon agora cobre 109 scripts / 110 `.uid` (**0 sem `.uid`**; 36 `.uid` de outras lanes stageados). A lane player-rig (`src/core/inventory/**` + `src/core/item.gd`, 7 `.uid`) saiu primeiro. Nota: a linha do `testkit` (#15) ainda dizia 32 — ficou defasada |
| **Hook gunsmith (inventário → "Modificar" → UI)** | player-rig + inventory-ux + range | **DONE end-to-end 2026-09-21T15:45Z** — `PlayerController`: `signal weapon_modify_requested` + `func request_weapon_modify()` (**COMMITADO addon `13b174c`**); `inventory-ux`: "Modificar" id 105 → `main.gd` → `player.request_weapon_modify`; **`range` VERIFICADO**: arena `close_inventory()` → `open_for_weapon` (5/5 PASS, import 0) |
| **Rig humanoide compartilhado player↔NPC** | player-rig (lado player) + npc-body | **interface FECHADA 2026-09-21T16:47Z; passo 1 + B1-B4 tratados** — cena **world mode** `humanoid_rig.tscn` (`Skeleton3D` 87 bones + `MESH` skinned + `AnimationPlayer`; SEM ik.gd/GodotIK/backpack/LookTargets/collider/near-clip) + classe `HumanoidRig` (`get_body_mesh`→no `MESH`, `set_tint`/`set_flash`, `set_lod`/`set_anim_enabled`/`set_visible_range`, `play`/`anim_names`, `head_node`/`chest_node`; anims `idle/walk/run/aim_idle/fire/reload/hit/death`). Passo 1: `ik.gd` → qualquer `CharacterBody3D`. **B2 (bot invulnerável) CORRIGIDO**: `PlayerBodyVisibility.apply(..., first_person)` gateia o tag; world mode não tageia/não instala nearclip (satisfaz B3). **B1**: `set_tint` (`modulate_color`) + `set_flash` (uniform `flash_amount` novo em `body_psx_base.gdshaderinc`). **B4**: `set_lod`. Feet Y medido (ankle local z=−0.784 → rig y=+0.784). parse 152/0, invariants 18/18. **Passo 2 EM CURSO**: classe `HumanoidRig` (`src/player/humanoid_rig.gd`) no disco (parse 153/0) com toda a API (`set_tint`/`set_flash`/`set_lod`/`own_materials`/`play`/`bone_global_position`/`GROUND_PLACEMENT_Y=0.851`). **CENA `humanoid_rig.tscn` + `humanoid_body_anims.res` NO DISCO 2026-09-21T17:00Z** (Skeleton3D 87 bones + MESH skinned, override=null, sem IK/backpack/LookTargets; lib com aliases `walk/run/aim_idle/fire/reload/hit/idle`); **ping feito, npc-body integrando**. C2 corrigido p/ **0.851**. **Falta M1**: `player_ik.tscn` ainda tem o proprio skeleton (herança de cena — passo seguinte, gate de invariants). Asserção INV-B2 (INV-17) feita pelo testkit — **19/19 PASS, provada por sabotagem**. **COMMITADO addon `c6c0e8d`** (rig: cena+classe+lib+ik.gd+body_visibility+shader) e `e8d8c71` (fix do autoload `Debug` p/ `--script`). **Aceitação do npc-body achou F6/F7/F9/F10 → corrigidos, commit `e327af3`**: F6 (flash: `own_materials` força o shader fork c/ `flash_amount`, cutoff 0), F7 (`GROUND_PLACEMENT_Y=0.893`), **F9 GRAVE (T-pose, pré-existente, o PLAYER também): 2393 tracks Mixamo→nomes do esqueleto retargeteados nos 16 clips**, F10 (loop nas ciclicas). Aguarda a 2ª aceitação do npc-body (gate + strip). **`reset` (sub-resource embutido, escapou do retarget por arquivo) corrigido em `19afb28`** + 78 tracks mortos removidos -> **24/24 clips dirigem >=1 osso** (INV-A do testkit desbloqueada). |
| **Hook gunsmith — VERIFICAÇÃO canal 2** | verifier | **DONE 2026-09-21T16:33Z** — 15/15 no caminho REAL: emit do `id_pressed(105)` real do menu → `BaseInventoryUI.request_modify_weapon` → `main.gd` → `request_weapon_modify` → `weapon_modify_requested` → `arena_manager._on_weapon_modify_requested` → `gunsmith_ui.open_for_weapon`; inventário FECHADO pelo handler; input de combate suprimido (`_pending_shots` descartado com a UI aberta); guard do null. Condições: jogo HEAD `1616785` / addon HEAD `32dc773`. Evidência `/tmp/shooter/gs_probe2.log`. **Achado de harness** (roteado ao testkit): nomear classe global dependente de autoload num runner `--script` força o compile antes dos autoloads → cache envenenado → player sem script → arena parece quebrada (falso-BLOCKED) |
| **roles (2 slices: estado por faccao + starter nao-transferivel) — VERIFICAÇÃO canal 2** | verifier | **DONE 2026-09-21T17:45Z** — 41/41 no caminho real (sem harness do autor): swap conserva **MASSA + multiset** em 6 trocas (0.098→0.098), PREP-only recusa `"raid em andamento"`, starter **1x por faccao**, **KIA forfeta só o kit ativo** (kit da outra facção byte-idêntico), `no_transfer` com guardas reais (market sell / barter `can_buy` / flea listing) e uma **cópia NORMAL passa nos 3**, flag no JSON sobrevive reload, save pre-`roles` carrega aditivo sem bump (`VERSION=2`). Condições: HEAD `ba67c92` (8 arquivos worktree==HEAD). Evidência `/tmp/shooter/vroles_probe.log`. INV permanentes propostos ao testkit |
| **Body rig compartilhado player<->NPC — requisitos do consumidor (NpcBot)** | npc-body | **CLAIMED by npc-body 2026-09-21T16:37Z** — spec do consumidor enviada em 2 vias: reply ao coordinator (`2026-09-21T16-36-25`, kind answer) e AMQ direto ao player-rig (`2026-09-21T16-37-39`, kind review_request) — 10 MUST (M1 cena unica sem duplicar mesh/skeleton; M2 rig world-mode SEM `ik.gd`, que escreve osso em `_physics_process`; M3 AnimationLibrary reaproveitavel + nomes semanticos idle/walk/run/aim_idle/fire/reload/hit/death; M4 helper de N meshes/superficies p/ tint/flash/fade com material por instancia; M5 NADA de near-clip/`HIDDEN_FROM_FPS_LAYER=4` no bot; M6 colisao capsule r0.35 h1.7 + resolucao do tiro para `NpcBot` ficam comigo; M7 paridade numerica olho 1.5/peito 1.2; M8 `died`/`died_with_team` + cadaver 10 s intactos; M9 valvula de LOD/anim por bot; M10 `bot.tscn`+`NpcBot` seguem a fachada publica) + 4 nice. **Nada de mesh/bone duplicado: fico no capsule ate a interface do player-rig chegar**; `src/player/` intocado. **2026-09-21T16:41Z: interface do player-rig RECEBIDA e ackada com contraproposta** (`humanoid_rig.tscn`/`HumanoidRig`, foot IK generalizado p/ qualquer `CharacterBody3D`) — 4 bloqueios com evidencia: **B1** M4 quebra em silencio (surface do `MESH` e `ShaderMaterial` em `player_mesh.tres:6`, entao `NpcVisuals.own_material()` cast p/ `StandardMaterial3D` = null -> sem tint/flash, sem erro; proposto `set_tint`/`set_flash` em `modulate_color` + uniform novo), **B2 HAZARD: `tag_own_colliders`/grupo `viewmodel` em NPC deixa o BOT INVULNERAVEL** (`shot_ray.gd:24-31` exclui o grupo no tree inteiro -> `shot_resolver.gd:43` nunca alcanca o `NpcBot`), **B3** `first_person=false` nao pode rodar `PlayerBodyVisibility.apply()` (nearclip+layer 4), **B4** valvula de LOD/perf (surface `cull_disabled`+prepass x 8 bots). Respostas: Q1 SIM (rig como FILHO do root NpcBot, capsule/collider ficam comigo); Q2 foot IK OFF (preciso do Y do rig); Q3 sem IK de mao (anims de rifle bastam); Q4 nome OK. **2026-09-21T16:43Z: interface PUBLICADA pelo player-rig e ackada** — `humanoid_body.tscn` + `HumanoidBody` (world mode: Skeleton3D+MESH+AnimationPlayer, sem IK/backpack/LookTarget/collider/nearclip = M1/M2/M5/M6/M10 OK), anims `idle/walk/run/aim_idle/fire/reload/hit` (sem `death` -> fallback rigido, nao bloqueia), `get_all_meshes`/`apply_instance_material`/`set_anim_enabled`/`set_visible_range`/`play`. **2 pendências bloqueantes:** **C1** `apply_instance_material` nao pode partir de `StandardMaterial3D` (surface do MESH e ShaderMaterial, `player_mesh.tres:6`) -> pedi `Material`/`ShaderMaterial` + `flash_amount` no fork `body_psx_base.gdshaderinc` (preferido) ou flash via `material_overlay`; **C2** falta o Y local do rig p/ os pes em y=0 (ref. Skeleton3D y~0.851 em `player.tscn:169`). M7: sigo com 1.5/1.2 ROOT-RELATIVE (`head_node()`/`chest_node()` so se eu quiser look-at, nunca no LOS). **2026-09-21T16:47Z: B1/B2/B3/B4 FECHADOS** — verifiquei no codigo (nao no papel): `apply(..., first_person=false)` = so `VISIBLE_LAYER`, sem nearclip/override/`tag_own_colliders` (`player_body_visibility.gd:32-41`, com o hazard do B2 documentado no proprio arquivo); `flash_amount` existe (`body_psx_base.gdshaderinc:208/245`, `ALBEDO = mix(ALBEDO, white, amount)`); `set_tint`/`set_flash`/`set_lod(0=full,1=anim congelada,2=invisivel)`/`set_anim_enabled`/`set_visible_range`. Nome final `humanoid_rig.tscn`/`HumanoidRig`. **Pendencia unica: C2 corrigido de volta ao player-rig** — ele propos `y=0.784` (poria o SOLADO ~6.7 cm ABAIXO do chao); o correto e **0.851**, o mesmo valor do player (`player.tscn:169`) porque ankle local z=-0.784 deixa 0.067 de solado. +3 ajustes pequenos (flash tambem em `EMISSION` p/ ler no escuro; paridade de shader player<->NPC; `death` bone-only sem root motion). **Aguardo o passo 2 dele (`humanoid_rig.tscn` + `HumanoidRig` no disco) para integrar: `bot.tscn` filho visual -> rig, `NpcVisuals.set_tint/set_flash`, anim por estado, `set_lod` por distancia**. **2026-09-21T16:51Z: classe `HumanoidRig` LIDA linha a linha no disco** (`addons/.../src/player/humanoid_rig.gd`) — `own_materials()` duplica por instancia em N meshes/surfaces sem mutar o recurso (M4/B1 OK), `flash_amount` presente (`body_psx_base.gdshaderinc:208/245/248`, com `EMISSION`), `GROUND_PLACEMENT_Y=0.851` (C2 fechado) e `EYE_HEIGHT/CHEST_HEIGHT` batendo. **3 achados devolvidos:** **F1** `set_visible_range` e NO-OP (l.111-112: so guarda o export, nada consome — valvula de perf publicada que nao funciona, a pior classe de falha); **F2** `set_anim_enabled` (l.106-109) ignora `_lod` -> `set_lod(1)` + `set_anim_enabled(true)` reanima o bot congelado; **F3** `_anim` so em `_ready()` e so entre filhos DIRETOS (l.38/138) -> `play`/`set_lod` viram no-op silencioso se o `AnimationPlayer` for aninhado. **+ requisito para a cena:** o `MESH` da `humanoid_rig.tscn` nao pode ter `material_override` (ganha do `set_surface_override_material` e mataria tint/flash calados; overlay e inofensivo) e tem que manter o material de `player_mesh.tres`. **F1/F2/F3 corrigidos pelo player-rig e CONFERIDOS por mim no codigo** (`set_visible_range` agora aplica `visibility_range_end` em `get_all_meshes()`, l.111-119; writer unico `_apply_anim_speed()` compondo `anim_enabled and _lod == 0`, l.123-127; `_ensure_anim()` lazy+recursivo, l.155-164) + resta **F4** (1 linha: `anim_names()` l.137 ainda le `_anim` direto, devolve vazio antes do `_ready`). **Aguardando a cena `humanoid_rig.tscn` (proxima acao dele) para integrar**. **F5 (nomes de animacao) enviado:** a lib existente (`animations_library.tres`) tem `walking`/`rifle_run`/`rifle_aiming_idle`/`firing_rifle`/`reloading`/`hit_reaction` — os nomes publicados (`walk`/`run`/`aim_idle`/`fire`/`reload`/`hit`) nao existem e **nao ha animacao `idle`**; pedi **alias** (mesma `Animation` em 2 keys) em vez de rename, para nao quebrar os AnimationTrees do player (folhas por nome em `resources/animations.res`, binario); meu lado tera tabela `semantic -> key` com fallback + asserção no probe**. **F1-F5 FECHADOS** — player-rig escolheu a rota ALIAS (nao renomeia keys: `walk`/`run`/`aim_idle`/`fire`/`reload`/`hit`/`idle` apontam para as MESMAS `Animation`s, AnimationTrees do player intactos) e eu conferi no disco que **F4 tambem esta corrigido** (`anim_names()` com `_ensure_anim()`, l.136-138). Os 10 MUSTs do consumidor estão cobertos pelo contrato acordado. **Gatilho unico restante: `humanoid_rig.tscn` + `humanoid_body_anims.res` (player-rig)**; ao chegar eu integro no mesmo turno (filho visual do `bot.tscn`, `NpcVisuals.set_tint/set_flash`, `semantic->key` com fallback, anim por estado, `set_lod`, `GROUND_PLACEMENT_Y=0.851`) e devolvo gate + strip do spotter**. **2026-09-21T17:08Z: artefato ENTREGUE e aceitacao RODADA** (`test/npc_rig_probe_tmp.gd`: **23 PASS / 3 FAIL**) — geometria/material OK (87 ossos, 1 surface, sem `material_override`, `own_materials()` duplica por instancia sem mutar o compartilhado, tint chega no `modulate_color`, aliases resolvem, F1/F2/F4 ok), mas **3 bloqueios medidos**: **F6** o shader que o NPC renderiza (`shaders/psx/psx_lit_alpha-scissor`) NAO declara `flash_amount` (o uniform so existe no fork do nearclip) -> `set_flash` seria no-op silencioso; **F7** `GROUND_PLACEMENT_Y=0.851` afunda o solado 4.2 cm (corpo no mundo y -0.042..1.744) -> pedi 0.893; **F9 (GRAVE, pre-existente, NAO e do rig)** os clips nao dirigem o esqueleto: 0 de 156 tracks casam (`Hips`/`LeftLeg` vs `spine_01`/`thigh.L_057`), `play(walk)` nao muda 1 osso, o **`player.tscn` tem a lib de animacao VAZIA** (viajar `Moving`->`Walking` muda 0 de 88 frames) e o GLB de origem nao tem AnimationPlayer. **Decisao: NAO troquei o `bot.tscn`** (T-pose = regressao visual vs capsule intencional) — escalei ao coordinator (urgent: esperar F9 ou aterrissar com T-pose documentada); F9 devolvido ao player-rig com caminho de fix (nomes atuais carregam o indice original como sufixo -> retarget mecanico). Invariantes INV-A/B/C oferecidos ao testkit**. **2026-09-21T17:14Z: F6 e F7 FECHADOS, so F9 resta** — player-rig corrigiu: `GROUND_PLACEMENT_Y=0.893` (`humanoid_rig.gd:28`, corpo no mundo y=[0.000, 1.786]) e `own_materials()` agora **troca o shader para o fork** (`psx_lit_body_nearclip.gdshader`, `body_near_cutoff=0`) -> o material EFETIVO declara `flash_amount` ✓. Meu probe de aceitacao: **25 PASS / 1 FAIL** (so F9). Armadilha registrada (foi a minha): medir `mesh.get_active_material(0)` ANTES de `own_materials()` devolve a surface compartilhada sem `flash_amount` -> falso-vermelho no F6. testkit promoveu **INV-18** (F7; harness 20/20) e escolhi **(a)** com ele: INV-B pode ir agora (verde), **INV-A segura ate o F9** (nao virar gate compartilhado vermelho por bug conhecido). F9 (0 de 3577 tracks casam; `play('walk')` = 5.2610 -> 5.2610) segue com o player-rig |
| Verificação: pitch (#2), IA (#3), quests/skills no caminho REAL (#4) | spotter | **#2 DONE**, **#4 strength RESOLVIDO 2026-09-21T14:43Z** — `Input.action_press` no caminho REAL (arena_blockout) dirige o player: 5/5 runs, 5.1–6.0 m, strength xp 0→1 (casa com 0.2 xp/m), persiste no readback. **Achado:** o "não anda" anterior era **bot congelado no caminho** (`Bot3`, `test_move blocked=true`), não o método de input — remover a colisão dos bots, não só parar o physics. Evidência `/tmp/shooter/spot_strength_verify.txt`. **#3 IA RESOLVIDO (slice) 2026-09-21T16:2xZ** — 13/13 PASS em probe independente: cobertura BLOQUEIA LOS de fato (ponto escolhido com parede real no meio por raycast próprio; posição inicial exposta como controle; campo aberto → NENHUMA cobertura falsa), tiers mensuráveis (RECRUIT 156 vs VETERAN 132 frames de física, diff 24 ≈ (1.4−0.25)·0.35), saque SÓ MARCA (`consumed=true` + id em `_looted`, nó vivo). Evidência `/tmp/shooter/spot_ai_verify.txt`. Ressalva RESOLVIDA: o loop EMERGENT do saque tambem foi fechado (8/8) — bot IDLE sem alvo ANDA 1.82 m até o loot e consome sozinho (seleção @phys 38, consumo @phys 63; nó vivo, `consumed=true`, 1 id em `_looted`). A falha anterior era do HARNESS (`patrol_speed=0` zerava a velocidade do loot-walk + contaminação do blackboard de squad), não do jogo. Evidência `/tmp/shooter/spot_loot_verify.txt`. 2º veredito independente (a pedido do verifier) — **faccao/migracao v1→v2 sob crash**: 22/22 PASS (blobs meta_profile `30d45ea` + profile_store `7c292a7`; save publicado sempre v1 OU v2 válido, currency 4242 preservada, 0 quarentena espúria, 3 kills na janela do `.tmp`); evidência `/tmp/shooter/spot_mig_verify.txt`. **Gunsmith (VISUAL) 2026-09-21T16:41Z** — painel aberto pela TECLA M real na arena (game `fc28ba9`, addon `23b16b0`): título "M4_Carbine — mounts: 3 montado(s)", 5 slots (MUZZLE/LEFT/RIGHT/TOP/UNDER); estado visual: só `Aimpoint_T1` (art YES, marker `Scope`) aparece no preview — `Harris_Bipod` e `A2_Flash_Hider` art NONE, confirmando o report "attachments não têm modelo"; captura `/tmp/shooter/spot_gunsmith_panel.png`. **HAZARD de harness** (doc no addon `23b16b0`): runner que NOMEIA classe global (`Weapon`/`GunsmithUI`) falso-bloqueia com "Debug not found" e envenena `.godot/global_script_class_cache.cfg` → usar duck-typing; regenerei o cache e `verify-all --quick` voltou a PASS (153 scripts). **FULL verify-all pós-rename 2026-09-21T17:12Z (HEAD `b1e1ab3`, árvore calma): FAIL** — `qa_audit` **14 BLOCKERs `ip-name`** remanescentes (Aimpoint em `resources/meta/traders/gunsmith.tres:21`; Imbel em `resources/weapons/Brazil_556.tres:8`; Dragunov em `resources/weapons/Dragunov_SVD.tres` filename+`:22`; Mossberg em `resources/weapons/USA_Pump.tres:5`; Beretta em `src/weapons/{material_pistol,material_silencer}.tres` + `weapon_pistol.tscn` ×7) + `inventory_ux` 1 falha (icones: 118 checks). Resto VERDE (import/parse 153, uid_tracking, assets 123, ballistics 40, weapon_mechanics 41, meta 73/34/45/35, invariants 19, factions 63). Log `/tmp/shooter/verify.528519/`, detalhe `docs/qa-report.md:66-79`. |
| Verificação PITCH da câmera (#2) | spotter | DONE 2026-09-21T11:09Z — 7/7 PASS (pitch -3/+3, clamp ±90, yaw ok; POI nivel 15m / baixo 4.18m / alto 10.14m; dot 0px a ±35°; ray segue o forward, resolve no Ground a 1.62m em -89°, não no corpo; lean 0.45m). Evidence /tmp/shooter/spotpitch*, report_pitch.md |
| `validate_invariants.gd` (**22** invariantes/checks cruzados permanentes) | testkit | **DONE — 22/22 PASS** headless. Provas de sabotagem: INV-07, INV-13, INV-12c, INV-17. Invariantes do rig compartilhado (aceitação do `npc-body`, 2026-09-21): **INV-18** (F7 sole on ground, `02e7e81`), **INV-19** (F6 material declara `modulate_color`+`flash_amount` — medir o material EFETIVO via `own_materials()` primeiro, senão falso-vermelho; `d143079`), **INV-20** (F9 todo clip dirige ≥1 osso do rig, `52e858c`). Commits (addon): `3e98608`+`53f178c`+`b795351`+`32dc773`+`c09cce4`+`02e7e81`+`d143079`+`64492cc`+`52e858c`; (jogo) `36d0fe7`+`7e44cec` |
| Ícones gerados dos modelos + UI/UX tetris do inventário | inventory-ux | **DONE 2026-09-21T14:29Z** — game `f2dbfa4` (gerador+assets), `ea7967f` (118 `.tres`), `e33990e` + `3226fb8` (regen pós-rename neutro; manifest 0 stale); addon `7d12d0f` (core tetris + `tooltip` + harness), `341cc3b` (fix right-click real), `a413f41` (harness: coverage do manifest nos dois sentidos). Gate `inventory_ux` **68/68 PASS**. Nota: `Belgium_BR` p/ placeholder (glb com dep stale — ballistics). Único vermelho do full gate: `qa_audit` BLOCKER=9 (outras lanes) |
| **Commit ballistics lane + .uid fix** | ballistics | **DONE 2026-09-21T13:52Z** — addon `9496847` (track .uid + ammo test arity fix), game `b8f1f5f` (ammo/armor UID repoint + 12ga); assets 123/123, ballistics 40/40 |
| **QA-003** (held weapon recoil, rule 5) | ballistics | **DONE 2026-09-21T14:11Z** — addon `ea41124`: `_apply_recoil` early-returns when `freeze` (held body); casing ejection untouched; `weapon_3d.gd.uid` tracked; other lane's uncommitted changes preserved via targeted stash; `check_scripts.gd` exit 0 (working tree) |
| **QA-020/021 NITs** | ballistics | **DONE 2026-09-21T14:17Z** — addon `cf68d56`: `##` docstrings in `item_3d.gd:78` + `cartridge_3d.gd:9/14`, corrected `calculator.gd:1` header; tracked both world `.uid`; parse gate 145 scripts / 0 failures |
| **QA-025 NITs** (6 stale addon headers) | ballistics | **DONE 2026-09-21T14:21Z** — addon `ef4ebda`: fixed headers in attatchments/firemode/reservoir/ammo_feed/utils/caliber_parser; tracked 4 untracked `.uid`; `attatchments.gd` isolated via targeted stash; parse gate 145 scripts / 0 failures |
| **Commit player-rig lane + .uid fix** | player-rig | **DONE 2026-09-21T13:56Z** — addon `ce68fda` (ViewmodelRig frozen-body gun pose, PlayerSurvival, body visibility/near-clip, remove `*.uid` from `.gitignore` + track 14 player `.uid`); verify-all --quick PASS (0 parse errors, 143 scripts, 123 assets) |
| **BLOQUEIO: HEAD tinha 15 weapons que não carregavam** (paths de script antigos + `ammofeed`) | ballistics (fix) + inventory-ux (icones) | **RESOLVIDO 2026-09-21T14:50Z, verificado por meta 14:51Z** — game repo `c35c780`: 15 `.tres` repontados para `src/core/weapon/weapon.gd` / `src/core/ammo/ammo_feed.gd` + `ammofeed` -> `ammo_feed`; split "worktree − icon" via `/tmp/shooter/strip_icon.awk`, icons da inventory-ux ficaram no working tree (não commitados); weapon_mechanics PASS, assets 123/123; **meta copiou o HEAD pós-commit e mediu CHK_SUMMARY checked=15 load_null=0 feed_null=0** |
| **Varredura de paths/typos em `resources/**/*.tres`** | ballistics | **DONE 2026-09-21T14:54Z** — `ea7967f` (inventory-ux) carregou também 15 repoints de `attachment.gd` -> `attachment/attatchments.gd` (conteúdo verificado correto pelo meta); scan de todos os `resources/**/*.tres`: 0 paths `.gd` referenciados ausentes no disco, 0 `ammofeed`/paths antigos de core restantes |
| **Attachments: wiring resource->modelo + 10 sem mesh** | ballistics | **DONE 2026-09-21T16:50Z** — addon `bc2d8fd` (`Attachment.model_scene`/`model_transform`; mount por marker em `Weapon3D`+`ViewmodelRig`; MAGAZINE via `Weapon.MAGAZINE_POINT`(-1) + `ammo_feed.max_capacity`, sem bit novo — reusa o sistema existente) + game `d0e82f9` (15 `.tres` com `model_scene`: 5 cenas existentes + 5 placeholders neutros por categoria). INV-16 PASS (15/15 com modelo; mag 30->40->30); invariants 18/18; check_scripts 152/0. Nomes de marca aguardam decisão do usuário |
| **Nomes neutros (marcas privadas)** | ballistics | **DONE 2026-09-21T16:58Z** — game `9ba9cf4` (25 resources renomeados para país+designação; `description` guarda a referência mecânica; refs em gunsmith.tres/validate_meta_persistence/generate_inventory_icons) + addon `48e90f6` (INV-16 -> USA_P40). Pendências de outras lanes: 1 ref comentada em `scenes/test_player.gd:10` (range), regenerar `manifest.json` (inventory-ux), linter `ip-name` (qa/testkit). 2º passe de calibres aguarda decisão do usuário |
| **2º passe: calibres neutros** | ballistics | **DONE 2026-09-21T17:02Z** — game `b1e1ab3` (8 ammo renomeados: .357/.40/.45/.380 Auto, 9x18mm, 9x21mm, 12.7x99mm, 7.62x51mm AP; refs em M1911/Brazil_556/armor) + addon `4e47eb1` (parser aceita Auto; certification .380 Auto). inventory-ux: regenerar `manifest.json` (paths de ammo tambem mudaram). Nota: o test `caliber_parser.gd` mantem as designacoes antigas de proposito (backward-compat do parser) |
| **Rename do modelo (weapon_parafal)** | ballistics | **DONE 2026-09-21T17:06Z** — game `8650bd6`: `assets/models/weapon_parafal` -> `weapon_battle_rifle`; `PSX_FN_FAL`/`T_FN_FAL` -> `Battle_Rifle`; 3 `.import` com hash MD5 do path recalculado (UIDs preservados); generator ref atualizado; 0 refs em cenas; duplicado de corrida removido. Icones gerados (fn_fal/mossberg_500/imbel_aglc) ficam com inventory-ux |
| **ip-name residual** | ballistics | **DONE 2026-09-21T17:15Z** — game `0e5a2a9` (commita os 6 deletions staged do weapon_parafal; `Dragunov_SVD` -> `SVD`; `gunsmith.tres` id `o_barter_aimpoint` -> `o_barter_sweden_r1`) + addon `485f2a1` (`viewmodel_rig._is_optic_marker` sem `acog`/`aimpoint`). Fora da lane: inventory-ux commitar paths/PNGs `Brazil_556`/`USA_Pump` (working tree ja neutro); range/asset: Beretta em `src/weapons` + filenames `weapon_pistol`/`weapon_ia2` + comentario Imbel em `scenes/test_player.gd` |
| **GLB battle rifle: strings de marca + re-import** | ballistics | **PARCIAL 2026-09-21T17:37Z** — game `ba67c92`: strings internas do `PSX_Battle_Rifle.glb` neutralizadas (`T_Battle`/`M_Battle_`/`SM_Battle`, length-preserving; estrutura do GLB validada); import cache stale limpo. **Bloqueado**: `godot --headless --path . --import` preso em `pistol_9mm_albedo.png` por concorrencia de varias lanes no mesmo `.godot`; inventory-ux regenera o icone do `Belgium_BR` quando o import passar |

| **Leva do ADDON da `inventory-ux`** (harness `validate_inventory_ux.gd` + `.uid`, `grid/container/item`, UI `main/container/slot/item`, `equipment.tscn`) | inventory-ux | **harness + deps FECHADOS em `7d12d0f`** (62/62; o gate `inventory_ux` fecha em clone limpo). Resta só a **UI em tela** (`src/ui/inventory/{container,main,slot,item}.gd` + `equipment.tscn`), entrelaçada com o `main.gd` do player-rig → vai no commit coordenado. `.uid`: addon HEAD **112** == index, **0** sem `.uid` (fechado) |

| **BLOQUEIO ESTRUTURAL DE CI: addons não estão commitados como gitlinks** | coordinator / infra | **ABERTO — achado pelo `meta` 2026-09-21T15:0xZ, medido em clone limpo** — `.gitmodules` declara 4 submodules, mas o HEAD **não tem nenhuma entrada de gitlink** (`git ls-tree HEAD \| grep addons` → nada; `git submodule status` → vazio; `git ls-files addons` → 0). Logo o passo do CI `git submodule update --init --recursive --depth 1` é **no-op** e o clone fica **sem `addons/`**: medi com `git clone` real → sem `addons/`, e o próprio script do gate 1 (`addons/cabra.lat_shooters/test/check_scripts.gd`) não existe ⇒ `verify-all` falha antes de qualquer harness. Ou seja: **o job `gates` do CI não tem como passar**, independentemente de token/`.uid`/refs. Duas saídas: (a) commitar os gitlinks (`git submodule add <url> <path>` / `update-index --add --cacheinfo 160000,<sha>,<path>`) — pina SHA do addon e exige commit de bump a cada mudança; (b) trocar o CI para `git clone` de cada addon na ref desejada (com token), sem pin. `src/meta/meta_service.gd` **está** no clone (a lane do meta não é afetada). **PRÉ-REQUISITO adicional medido pelo `meta` 15:1xZ**: **nada de hoje foi pushed** — o addon `cabra.lat_shooters` está **9 commits à frente** do `origin/main` (`7d12d0f` vs `843bb25`) e o **repo do jogo 15 commits à frente**; os outros 3 addons estão em sincronia. Logo a opção (a) **não funciona antes do push do addon** (o SHA pinado não existe no remote → o clone do CI falha no fetch), e a (b) pegaria um addon antigo. Ordem: push do addon → (a) ou (b). Push é do usuário/coordinator (regra 1). `testkit` já tornou a falha honesta com o preflight (`f7a8730`, medido: FATAL + exit 1 em 0s em clone sem addons). |

| **Refactor "faction é conteúdo" (String id + pacote)** | range (dono) → toca o file-set do meta | **EM VOO 2026-09-21T16:1xZ** — `faction` deixou de ser enum e virou **String id** num pacote (`resources/meta/factions/{contractor,drifter,raider}.tres` + `scenes/faction.gd` + `FactionRegistry`); `ExtractionPoint` já migrou para `allowed_factions.has(profile.faction)` (sem String-vs-enum). Caiu no file-set do meta: `src/meta/meta_profile.gd` tem 2 hunks do `range` (não commitados) e `resources/meta/factions/` é novo/untracked. **Recomendação do meta (atomicidade, não território): o `range` commita esses 2 hunks junto do pacote** — separar deixaria o HEAD gravando `int(faction)` sobre String. Verificado pelo meta: `faction_from_saved` correto (String direto · número legado migra com `push_warning` · desconhecido `push_error`+default) e **os 4 gates do meta passam com a mudança** (38/34/45/35). `VERSION` fica **1** de propósito (leitura retrocompatível); o bump v1→v2 **com migração** é do meta e é pré-requisito do `roles` |

| **Migração/versionamento do save (v1→v2) — liberado pelo coordinator** | meta | **DONE 2026-09-21T16:26Z — `ef69f57`** — `MetaProfile.VERSION` 1→2 + `migrate()` (passos explícitos; passo ausente = HARD STOP; guarda de loop) + `_migrate_1_to_2()` (faction: índice legado → String id via `faction_from_saved`, sem adivinhar); `ProfileStore` **migra** save antigo em vez de quarentenar (futura/sem versão continua quarentenando; migração que não fecha em VERSION quarentena). Teste "save v1 REAL sobrevive ao load": persistence **51/51** (era 38) — faction 1→"drifter", stash/massa/currency/loadout/contadores/EXP preservados, **sem `.corrupt-*`**, reescrito como v2; índice fora de faixa (99) → erro alto + default, também sem quarentena. progression 34/34 · market 45/45 · flea 35/35 · `validate_invariants` **16/16** · `verify-all --quick` PASS. Atomicidade resolvida: o `range` commitou os 2 hunks dele em `20f812b`, então o commit do meta ficou só com a migração (sem varrer outro lane) | — `MetaProfile.VERSION` 1→2 + `migrate()` (passos explícitos; passo ausente = HARD STOP) + `_migrate_1_to_2()` (faction: índice legado → String id via `faction_from_saved`, sem adivinhar); `ProfileStore` agora **migra** save antigo em vez de quarentenar (futura/sem versão continua quarentenando). Teste "save v1 REAL sobrevive ao load": persistence **51/51** (era 38) — faction 1→"drifter", stash/massa/currency/loadout/contadores/EXP preservados, **sem `.corrupt-*`**, reescrito como v2; índice fora de faixa (99) → erro alto + default e também sem quarentena. progression 34/34 · market 45/45 · flea 35/35 · `validate_invariants` **16/16**. **BLOQUEIO de atomicidade**: `meta_profile.gd` também tem 2 hunks do `range` (faction String) não commitados e **sem eles o commit do meta quebra** (`to_dict()` faria `int(faction)` sobre String) → pedido ao `range` para commitar os hunks dele (recomendado) ou autorizar inclusão; coordinator avisado com a opção "inclui" |
| **Moeda neutra (identidade)** | meta | **DONE 2026-09-21T16:19Z** — commit **`c52d1ad`**: `₽`/`"RUB"`/`roubles` → `"%d cr"`/`"CR"` no `offer_line`/quest/raid report/flea/listing/`trader.currency` (3 `.tres`) e mensagens do harness; `grep ₽\|rouble\|RUB src/meta resources/meta` = vazio. Flag levantada pelo `range` (o `offer_line()` aparece no HUD da arena). Nada comparava o id da moeda; gates idênticos |

| ~~**2 harnesses órfãos (existem, passam, e o gate nunca roda)**~~ | testkit (dono do `verify-all.sh`) | **RESOLVIDO 2026-09-21T16:5xZ** — `factions` ligado em `16859b7` e **`meta_flea` em `1b91e89`**; `verify-all` **FULL PASS** com `meta_flea 35 checks` e `factions 63 checks`. Varredura do `meta` (todo `validate_*.gd` no disco × o que o gate chama) achou os dois: `scenes/validate_factions.gd` (do `range`) e `src/meta/validate_meta_flea.gd` (**do meta** — criado depois do gate). `validate_util.gd` fica fora (helper `RefCounted`). NIT do log resolvido: `gate_harness` decide por **exit code + linha `RESULT: PASS`** (não varre log por erro) + nota no header; e o `range` corrigiu a fonte do erro de startup do autoload (`fc8d5dd`, acesso guardado a `/root/Debug`) | — varredura "todo `validate_*.gd` no disco × o que o `verify-all` chama" (9 wired) achou 2 reais: **`scenes/validate_factions.gd`** (do `range`, **63/63 PASS**, não referenciado em `tools/`/`.github/`) e **`src/meta/validate_meta_flea.gd`** (**35/35 PASS**, **do meta** — criei o harness depois do gate e nunca registrei). Classe "código correto que ninguém chama": a cobertura existia e não rodava. `src/meta/validate_util.gd` **não** entra (é helper `RefCounted`, falso positivo da varredura). **NIT para quem ligar o `factions`**: toda rodada emite no startup `Identifier not found: Debug` (`controller.gd:703`) → `extraction_point.gd` "Failed to compile depended scripts" → `Compilation failed`, e **mesmo assim** sai exit 0 / 63 PASS (o autoload `Debug` não existe em modo `--script`; reprodutível 2/2). As checagens são reais (cenário [5] afirma aceita/recusa por facção com o texto do pack). **RESOLVIDO o NIT: o `gate_harness` decide por `rc == 0` E `grep -qE 'RESULT: PASS'` — NÃO varre o log por erro**, então "gritar" não vira FAIL falso (verificado pelo `meta` no `verify-all.sh` e pelo `verifier`). **Estado do wire: `factions` está ligado apenas no WORKING TREE (`HEAD: 0`, arquivo dirty) — commit pendente do `testkit`; `meta_flea` ainda não está ligado em lugar nenhum.** Os 6 `.opencode/skills/godot-testing-patterns/scripts/*.gd` são amostras de skill (`GdUnitTestSuite`), fora do gate por design |

| **`roles` slice 1 (estado por facção) — FEITO** | meta | **DONE 2026-09-21T16:56Z — `764425d`** — `roles` keyed por id de facção (`kit/karma/raids/survived/kia/starter_granted`); `role_kit()` devolve o `loadout` VIVO para a facção ativa (uma cópia só); `switch_faction()` é **swap de dois slots, nunca cópia** (massa conservada 7.0430→7.0430 em 2 trocas); `MetaService.set_active_role()` recusa com raid preparado/rodando; `grant_starter_loadout()` **1× por facção**; contadores por facção; KIA forfeta **só o kit ativo** (kit da outra facção byte-idêntico). Persistência **ADITIVA** (default `{}`, **sem bump** — leitura não precisa). Save pré-`roles` com gear conta como `starter_granted` (sem faucet em save migrado). Teste: persistence **73/73** (era 51) · progression 34/34 · market 45/45 · flea 35/35 · invariants 18/18 · factions 63/63 · check_scripts 153/0 |
| ~~**PENDÊNCIA do mesmo pilar: `no_transfer` do gear inicial (FAUCET ABERTO)**~~ | meta | **DONE 2026-09-21T17:25Z — `58c4ba0`** |

| ~~**`inventory_ux` VERMELHO no HEAD (bloqueava o `verify-all` completo)**~~ | inventory-ux | **RESOLVIDO 2026-09-21T17:30Z** — game `e33990e` + `3226fb8` regeneraram o manifest (**0 chaves stale**) e o addon `a413f41` deixou o check **bidirecional**: enumera `resources/**/*.tres` e falha separadamente por **coverage** ("not in manifest") e por **staleness** ("stale manifest key (file gone)") — a opção (b) que o `meta` sugeriu, implementada melhor: exige ícone real **ou placeholder documentado** (`missing_model`). Verificado pelo `meta`: **68/68 PASS**, e varredura própria do manifest = **0 stale**. Lição: o check antigo resolvia por lista, então um *rename* aparecia como "ícone faltando" (sintoma errado) — agora falha pelo motivo certo |: não é `.godot` nem corrida (3 rodadas estáveis: 65 passed / 1 FAIL). É o **manifest rastreado**: `_check_generated_icons_wired` faz `load()` de **cada chave** de `assets/ui/inventory/generated/manifest.json`, e **a única chave que não resolve é `res://resources/weapons/Dragunov_SVD.tres`** (2 ocorrências; renomeado para `SVD.tres` em `0e5a2a9`). Varredura do meta: todas as outras chaves resolvem e **todos os PNGs referenciados existem** → é literalmente 1 chave/2 linhas. Está no HEAD também (regeneração `e33990e` não pegou esse rename) ⇒ **clone limpo também fica vermelho**. Não é bug do gate do `testkit`; `.godot` **não** foi purgado (compartilhado, lanes rodando) |

| ~~**PENDÊNCIA do mesmo pilar: `no_transfer` do gear inicial (FAUCET ABERTO)**~~ | meta | **DONE 2026-09-21T17:25Z — `58c4ba0`** — starter gear é **não-transferível**: `ItemCodec` carrega o flag (sobrevive a save/reload), `TradeOps.is_transferable/count_transferable_in_stash` + `take_from_stash` **pula cópias não-transferíveis** (senão a venda "dava certo" e levava a cópia errada), e **todo** caminho de transferência usa isso: `Market.can_sell/sell`, o requisito de barter em `can_buy` e `FleaMarket.list_from_stash`, com recusa explícita `"item nao-transferivel (starter)"`. Equipar/usar continua funcionando; uma cópia **normal do mesmo path** segue vendável (a regra é o flag, não o path). Teste: market **53/53** (era 45), +8 checks no cenário [H]. **Faucet fechado** |
| **BLOQUEIO momentâneo: `src/npcs/bot/bot.gd` com erro de sintaxe no worktree** | npc-body | **ABERTO — achado pelo `meta` 17:25Z, owner avisado** — `Parse Error: Expected statement, found "else" instead` → `NpcBot` não parseia → `wave_spawner` falha em cadeia; `check_scripts` = **154 scripts / 6 failures** e `validate_invariants` cai de 18/18 para **14/7** (a cena de player não instancia ⇒ 7 invariantes de player falham; **zero** do meta). Arquivo `M` (WIP não commitado) — se for isso, é transiente; se for commit, é **CI vermelho global** |

| ~~**BLOQUEIO momentâneo: `src/npcs/bot/bot.gd` com erro de sintaxe no worktree**~~ | npc-body | **RESOLVIDO/TRANSIENTE 2026-09-21T17:28Z** — era WIP do `npc-body` (trocando o corpo dos NPCs); confirmado pelo `coordinator` e **verificado pelo `meta`**: `check_scripts` agora **154 scripts / 0 failures / RESULT: PASS, exit 0** (o arquivo segue dirty mas parseia). Não era CI vermelho. Registro do método: eu tracei a cascata (NpcBot → cena de player → 7 invariantes de player) antes de atribuir, em vez de acusar o meu próprio commit |
| **Pilar dos papéis: ENTREGUE (estado por facção + `no_transfer`)** | meta | **DONE 2026-09-21** — `764425d` (estado por facção: swap conservativo por massa, starter 1×/facção, contadores, KIA isola o kit inativo) + `58c4ba0` (starter não-transferível: fecha o faucet). Gates: persistence 73/73 · market 53/53 · flea 35/35 · progression 34/34 · invariantes do meta (INV-12a/b/c, 13, 14, 15) PASS. **Próximo passo DEFERIDO pelo coordinator** (karma consumindo conteúdo / UI de escolha na entrada) até fechar a rodada de corpo dos NPCs. **Único ponto ainda não verificado em canal-2** (spotter/verifier cobriram mercado/flea/facção, não os `roles`) — verificação independente oferecida |

## VERIFICADO (implementação + verificação independente)

**Movimento** — walk/sprint/crouch/prone/jump/queda; **lean que faz peek de verdade** (câmera translada
0.45 m com clamp de 0.20 m na parede; roll 8° secundário; corpo inclina 10°).

**Câmera / corpo em primeira pessoa** — pitch chegando ao rig da câmera (clamp exato ±90°, yaw
intacto) validado **com POI por inclinação**: nivelado 15 m, para baixo 4.18 m, para cima 10.14 m, com
o raio do resolver casando com o da câmera nos três. Dot a 0.0 px em ±35°. Corpo: **split por OSSO**
(`BodyMeshSplit`, cabeca+pescoco em malha propria na layer 4 = fora da camera FPS, PiP mostra tudo;
buffer de vertices compartilhado, pesos intactos) — sem cabeca/nuca/rosto na vista FPS e sem interior
de cranio, braco+arma visiveis a pitch 0. Cutoff de distancia reduzido a 0.05 (so near plane).
**Limitacao de asset em aberto**: a malha e de 3a pessoa e o TORSO volumoso oclui os pes quando se olha
para baixo — decisao de design do coordinator (aceitar / esconder o tronco tambem / re-autorar).
O tiro **nao** acerta o proprio corpo em pitch extremo (raio ingenuo acertava o `TorsoAttachment` a
0.51 m; com as exclusoes nao). *Nota de cobertura: toda verificacao de mira/POI/ADS deve declarar o
pitch usado — as antigas, todas a pitch 0, eram corretas e incompletas.*

**Gunplay** — 18 armas, 50 munições, 23 armaduras, 15 attachments. **Recht-Ipson** `Er=max(0,Eh−Ebl)`
+ **Poncelet** `P=K·ln(1+E/E1)` + LOS `t/cos θ`. Cert tables decidem o stop, física é fall-through.
Placas em camadas, destrutibilidade por material, mínimo 1 de durabilidade por projétil, reparo que
corta a durabilidade máxima. **Multi-projétil real** (buckshot = N projéteis, cada um rolando
armadura/dano/sangramento). **ADS com POI=POA a 15/30/50 m** (M4 e AK), alinhamento derivado do sight
marker. **Falhas de arma** (feed/stovepipe/misfire por munição × desgaste), limpeza com tempo por tipo
(tecla X), desgaste por tiro, ergonomia no tempo de ADS.

**Sobrevivência** — stamina (dreno por peso, gate de sprint, tremor), peso real (o wrapper não copiava
massa: tudo pesava 0), sangramento leve/pesado que **mata**, fratura persistente (só splint), dor +
analgésico, membro preto + cirurgia, energia/hidratação, HUD de status.

**IA** — percepção (**cone 110°** + **audição 30 m** + estados IDLE→SUSPICIOUS→ENGAGED→SEARCH),
cobertura que escolhe ponto bloqueando LOS, **squad** com blackboard por time, saque de loot (só marca),
**tiers** RECRUIT/REGULAR/VETERAN (reação 30 vs 6 frames), times + waves + friendly-fire, debug visual.

**Modos e loop de raid** — `GameMode` plugável (FFA/TDM, placar e win condition por modo); raid com
timer, **6 tipos de extração** (instantânea, paga V-Ex, flare, alavanca, co-op, timed) com facção,
single-use e item exigido; outcomes **SURVIVED / RUN_THROUGH (<7 min e <200 EXP) / MIA / KIA**;
**raid event bus**.

**Persistência** — stash + perfil com save **versionado e atômico** (temp+rename, quarentena `.corrupt`),
autoload único `Meta` como autoridade. **Verificado cross-process**: extrair com itens sobrevive a
fechar o jogo; **KIA deixa o stash byte-idêntico** e perde o loadout. Extração paga gasta a **carteira
persistente**. "Voltar ao menu" não perde o save.

**Progressão** — skills use-based nos ganchos existentes (`endurance_level`/`strength_level` chegam ao
survival), persistidas; quests data-driven consumindo o bus, com **RUN_THROUGH não contando** objetivo
de extração e recompensa caindo no perfil/stash. **Traders** com loyalty (nível+reputação), barter,
recusas claras, reputação por trade/quest e **seguro** (manifesto, devolução após prazo, exclusões).

**Apresentação** — menu + settings (sensibilidade, volumes por bus, pixelização PS1), inventário com
tooltip e quick-equip, HUD sem crosshair (mira nas ópticas), áudio unificado (12 SFX CC0, buses+pools,
`master.sh`), consistência visual (`shared_env`/`shared_ground_material`/`hud_style`). Perf: HUD 10 Hz,
flash cacheados, ShotRay com rescan fora do per-shot — ~18-21 ms com 3 bots em VGL/940MX.

**Entrega** — `export_presets.cfg` (Linux/X11 + Windows), export headless **funcionando** (Linux ~121 MB,
Windows ~155 MB, smoke exit 0), templates de export no `flake.nix` (devShell linka), CI com job de gates
sempre e export condicional (submódulos privados exigem token). README de framework.

**Qualidade** — `validate_assets` (123), `validate_tarkov_ballistics` (40), `validate_weapon_mechanics`
(41), `validate_meta_persistence` (38), `validate_meta_progression` (34), `validate_meta_market` (42),
`validate_meta_flea` (34);
`tools/qa/audit.mjs` com gate
por severidade; `tools/amq-herdr-bridge.mjs` fechando entrega.

## DÍVIDA CONHECIDA (do `qa`; baseline v2.1.0: **BLOCKER 0** / MAJOR 99 / MINOR 51 / NIT 19)

- **INV-A RESOLVIDO (F9, `player-rig`)** — a asserção "todo clip da `humanoid_body_anims.res` dirige ≥1 osso do
  rig" foi **promovida como INV-20** (addon `52e858c`): `player-rig` retargetou os 23 clips `.tres` (`e327af3`) e
  o `reset` EMBUTIDO (`19afb28`, sub_resource, não `.tres`); medido `clips=24 without_a_match=0`. Harness
  `validate_invariants` = **22/22 PASS**.

Gate: `node tools/qa/audit.mjs --check` (exit 1 só com BLOCKER; exit 2 se MAJOR subir do baseline).
**Política de baseline:** o baseline commitado é a referência e o CI compara contra ele — re-baselinar é
ato deliberado (junto com a decisão de aceitar a mudança), nunca automático. Taxonomia em 3 classes:
`morto-real` → dívida/MAJOR · `consumidor-pendente` → informativo com dono · `falso-positivo-de-scan` →
bug da ferramenta (não conta; falso positivo confirmado pelo dono vai em `tools/qa/ignore.json`).

- **rule-5** (`world/weapon_3d.gd:221`) — arma na mão recebe força/torque por tiro: viola AGENTS regra 5
  e duplica o recoil do `ViewmodelRig`. Fix: deletar `_apply_recoil`, uma fonte só.
- **mass divergente** (`core/inventory/container.gd:28/52`) — `total_weight`/`can_add_item` usam
  `item.mass` (0 no wrapper) enquanto `get_total_mass` usa `get_mass()`. Limite de peso cego ao conteúdo.
- **broken-refs** (3) — caminhos `res://` que não existem, em `.tscn`/`.tres`. Erro latente de export
  (o caso `world_ground` já mostrou). Roteados por dono pelo `qa`.
- **HAZARD DE MÉTODO: diff driver externo do repo** — `git diff` puro **não emite** linhas `+`/`-` (imprime o box desenhado), então uma checagem "o diff só tem X" pode sair **vazia** e passar vacuamente (aconteceu com o `meta`: "0 linhas não-ícone" contra um `--stat` de 118 arquivos). Forma segura: `git -c diff.external= diff --no-ext-diff --unified=0 <de> <para> -- <paths> | grep -E '^[+-][^+-]'`. Foi assim que se falsificou o "icon-only" do `ea7967f` (que trazia 15 repoints de `attachment.gd` → `attachment/attatchments.gd` + 7 trocas de fonte de ícone; conteúdo correto, commit não era o que dizia ser).
- ~~**ícones médicos: ext_resource para PNG untracked**~~ — **RESOLVIDO** por `f2dbfa4` (assets) + `ea7967f` (wire dos 118 `.tres`). Verificado pelo `meta` no HEAD de agora: **117 refs de ícone, 0 sem PNG rastreado**; e **33/33 weapons+attachments carregam** (probe com cópia do HEAD para `user://`).
  Achado original (meta 14:44Z): os 9 `resources/medical/*.tres` referenciavam PNG untracked; a pasta tinha
  0 arquivos rastreados e não estava no `.gitignore`. Lição: **nenhum gate local via isso** — `uid_tracking`
  cobre só `.gd`↔`.uid` e o `broken-ref` do qa só dispara quando o alvo **não existe no disco**
  (`audit.mjs:735-750`), e o PNG existia local. Só o clone limpo (CI) pega. **Correção de desenho**: o check
  "barato" de cruzar `path=` com `git ls-files` foi **falsificado pelo testkit** — `ext_resource` resolve por
  **`uid=` primeiro**, então path-only dá falso-positivo (uid válido + path stale CARREGA) e não vê o caso
  inverso; e a declaração de uid mora no `.import`/`.uid`, às vezes no **outro repo** (addon nested). Regra
  correta: para cada `ext_resource`, uid resolve (tracked) **OU** path resolve (tracked), com o mapa de uid vindo
  de `.uid`+`.import` dos **dois** repos. Fatal só quando uid desconhecido **E** path inválido (o caso dos 15
  weapons). Varredura uid-aware do meta: 148 refs vs 507 declarações → 19 sem declaração COMMITADA, todas
  declaradas no disco (`.uid` em voo) e **benignas** (path válido). Insumos em `/tmp/shooter/uidaudit2/`.
- ~~**BURACO DE COBERTURA no `INV-12` (gate permanente): direção "versão ANTIGA migrável"**~~ — **RESOLVIDO
  2026-09-21T16:34Z** pelo `testkit` no addon **`32dc773`**: `INV-12c old_save_migrates_not_quarantined` grava
  um save v1-shaped (`version:1, faction:1` índice legado, `currency:4242`) e exige `currency == 4242`,
  **`faction == "drifter"`** (não o default — prova que a tradução rodou, não um fallback silencioso), sem
  `.corrupt-*`, e re-save com `version == VERSION`. **Prova de sabotagem do testkit**: reverter o `ProfileStore`
  para `version != VERSION → quarentena` faz o INV-12c **FAIL** (16/17, exit 1); revertido e limpo. O `meta`
  verificou de forma independente: **17/17 PASS**, `grep SABOTAGE` vazio e `profile_store.gd` == HEAD (sem
  sabotagem esquecida no repo compartilhado)
- **dead-API (morto-real)** — MAJOR em triagem; `qa` vai dar o breakdown por regra/dono (`unused-func`
  que retorna valor · `unused-field` · `dead-api` só-escrito) para não mandar dono caçar fantasma.
- **god objects** — `arena_manager` ~1147, `controller` ~1178, `cartridges_3d` ~1007. Não bloqueiam.
- ~~**duplicação** arena↔debug_range~~ — **RESOLVIDO (range) 2026-09-21**: `ViewSetup.configure_fps` + `Footsteps.tick` + `ShotResolver.resolve` + `ShotResolver.apply_hit` compartilhados; `ShotRay.GROUP` aliasa `PlayerBodyVisibility.SHOT_EXCLUDE_GROUP`; helpers duplicados removidos (dup arena↔range -> 0). `scenes/cartridges_3d.gd` (demo @tool orfao, 1007 linhas) **deletado** -> dup -> 0. Harness: dedup 6/6 + shot funcional HIT 6m 1793J (bot 560->490); import 0, boots ok. Baseline qa: 0/50/37/14 (deve cair p/ ~48 apos o delete).
- **corpo em primeira pessoa** — pitch/self-hit entregues e verificados; falta o **split de malha por
  osso** (cabeça+pescoço ocultos só da câmera FPS) para "ver os pés" de verdade — o corte por distância
  provou-se errado nos dois valores testados (mostra a nuca ou o próprio rosto).
- Guara scope housing (asset placeholder mal-baked) — parked.
- **meta: persist e do chamador** (`src/meta/*`) — as APIs de `MetaProfile`/`Market`/`FleaMarket` mutam
  estado em memória; persistir é responsabilidade do chamador (`Meta.persist()`; a arena chama em
  `_market_buy`/`_market_sell`). Documentado e **intencional**, verificado (spotter 2026-09-21). Risco:
  quem usar a API direto e esquecer o `persist()` perde a operação ao fechar. Endurecimento opcional
  (auto-persist nos wrappers do `MetaService`) = mudança separada, exige re-verificação. Não é bug.

## PRÓXIMO (não iniciado)

- **Gate uid-aware de `ext_resource` (fatal-only)** — desenho acordado com o `meta` (2026-09-21): para cada
  `ext_resource`, "o uid resolve para arquivo rastreado" OU "o path resolve para arquivo rastreado", com o mapa
  de uid de `.uid` + `uid=` de `.import` dos DOIS repos. O caso fatal e exatamente "uid desconhecido E path
  invalido" (os 15 weapons corrigidos em `c35c780`). NAO e so-path (falso-positivo: uid resolve primeiro) nem
  so-uid. Custo real > 20 linhas e encosta na regra `broken-ref` do qa — nao construido. Insumo do sweep do meta:
  `/tmp/shooter/uidaudit2/` (19 sem declaracao commitada mas declaradas no disco; 0 "nowhere").
- **Segundo mapa** — o teste real da premissa "isto é um framework": montar um nível novo só com as peças
  existentes (env compartilhado, `Raid`, `GameMode`, spawns, extrações, bots).
- **Escolha do `meta`**: hideout (DAG de módulos + timers) · flea market (listings/taxa/escrow) · **segundo papel
  (Contractor/Drifter) — ESCOLHIDO e aprovado pelo coordinator 2026-09-21T15:49Z**: UM save / um `MetaProfile`
  com estado **por papel** (não dois perfis), escolha do papel na **entrada do raid**, extrações via
  `ExtractionPoint.faction` (já existe), karma separado do rep de trader. Levantamento aterrado em
  `docs/meta-second-role-survey.md` (commit `66e10f3`): modelo de estado, fronteira gear/loot entre papéis
  (banco compartilhado + kits separados, swap só em PREP, conservação por massa, kit inicial 1x/papel e
  não-transferível, KIA forfeta só o kit ativo), karma aditivo-only, entrada. **PRÉ-REQUISITO BLOQUEANTE
  achado**: `ProfileStore` quarentena versão desconhecida (não migra) → bump p/ v2 sem migração v1→v2
  **apaga todo save existente**. NÃO iniciado: aguarda `verifier` fechar mercado/flea + liberação do coordinator.
- Guias por sistema em `docs/` (README cobre o "como adicionar"; faltam os "como funciona").
  **meta FEITO 2026-09-21**: `docs/meta-systems.md` (commit `df143cf`) — contrato de persistência, resolução
  de raid por outcome, traders/flea/seguro, skills/quests, como adicionar conteúdo e a dívida do `persist`.
  Faltam os equivalentes de gunplay/IA/apresentação.
- Shaders PSX nos materiais (precisa de julgamento visual), netcode (Fase 4).

Árvores sujas nos dois repos — **commits autorizados nesta rodada** (ordem do usuário via coordinator).
Evidências em `/tmp/shooter/`. testkit: `3e98608` (addon, test toolchain: parse gate + validators +
`validate_invariants` 10/10 + Jev oracle) e `36d0fe7` + `45e276b` (jogo, `tools/verify-all.sh` 10 gates + `.github/workflows/ci.yml`).

**Estado atual do gate:** `uid_tracking` **VERDE** (0 missing; addon HEAD `.uid`=112 == index). **Dependências de CI:** `tools/qa/*` + `docs/qa-baseline.json` **FECHADO** (qa `94c42a5`+`fbdcab1`). **BLOQUEIO ESTRUTURAL ainda ABERTO (meta):** o HEAD do jogo não tem gitlink (0), então um clone limpo NÃO tem `addons/` — o `verify-all` falha com um preflight claro (`f7a8730`) em vez de "Can't load script"; logo o PASS do gate é **working-tree**, não CI; fix = infra (gitlinks ou CI clonando os addons). Verificação em paralelo: `VERIFY_LOG_DIR` próprio (dir por-PID por padrão) + `flock` por-repo (`70e455a`).
